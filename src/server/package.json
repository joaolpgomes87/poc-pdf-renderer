{
  "name": "pdf-renderer-server",
  "version": "1.0.0",
  "main": "dist/server.js",
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "puppeteer": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "@types/express": "^4.17.13",
    "@types/cors": "^2.8.12",
    "@types/node": "^16.11.7",
    "ts-node": "^10.4.0",
    "nodemon": "^2.0.15"
  },
  "scripts": {
    "start": "node dist/server.js",
    "build": "tsc",
    "dev": "nodemon --exec ts-node server.ts",
    "clean": "rm -rf dist",
    "build:clean": "npm run clean && npm run build"
  }
} 